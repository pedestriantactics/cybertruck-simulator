extends Node

# get the audio effect phaser from the bus named "Spatial"
@onready var phaser = get_node("Spatial/Phaser")

# Called when the node enters the scene tree for the first time.
func _ready():
	# get the parent node and connect the signal 
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
